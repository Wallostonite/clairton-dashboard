# Pinned to the versions verified against this code. Native Streamlit charts
# replace matplotlib, so that dependency has been dropped.
streamlit==1.51.0
pandas==2.3.3
pyarrow==21.0.0
openpyxl==3.1.5   # optional: lets the ETL read .xlsx exports too
